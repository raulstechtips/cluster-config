---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: serenemind-postgresql-scheduled-backup
  namespace: serenemind
spec:
  cluster:
    name: pg-serenemind
  schedule: "0 0 0 * * *"
  backupOwnerReference: none
  method: volumeSnapshot
  immediate: true
