---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: infisical-secret-harbor-redis-secret
  namespace: harbor
spec:
  hostAPI: "${ARGOCD_ENV_INFISICAL_PROD_HOST_API}"
  authentication:
    universalAuth:
      credentialsRef:
        secretName: universal-auth-credentials
        secretNamespace: infisical
      secretsScope:
        projectSlug: "${ARGOCD_ENV_INFISICAL_PROD_PROJECT_SLUG}"
        envSlug: "${ARGOCD_ENV_INFISICAL_PROD_ENV_SLUG}"
        secretsPath: "/harbor/redis"
        recursive: true
  managedKubeSecretReferences:
    - secretName: harbor-redis-secret
      secretNamespace: harbor
      secretType: Opaque
      creationPolicy: Owner
      template:
        includeAllSecrets: false
        data:
          REDIS_PASSWORD: "{{ .HARBOR_REDIS_PASSWORD.Value }}"
