---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strict-referrer
  namespace: serenemind
spec:
  headers:
    # Allow full Referer on same-origin; send only origin on cross-origin
    referrerPolicy: strict-origin-when-cross-origin
