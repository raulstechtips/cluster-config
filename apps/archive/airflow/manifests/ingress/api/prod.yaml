---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: airflow-api-ingress-prod
  namespace: airflow
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(``)
      kind: Rule
      middlewares:
        - name: strict-referrer
          namespace: default
        - name: default-headers
          namespace: default
      services:
        - name: airflow-api-server
          port: 8080
  tls:
    secretName: prod-raulstechtips-tls
