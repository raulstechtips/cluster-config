---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: prometheus-ingress-prod
  namespace: monitoring
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`${ARGOCD_ENV_PROMETHEUS_PROD_DOMAIN}`)
      kind: Rule
      middlewares:
        - name: strict-referrer
          namespace: default
        - name: default-headers
          namespace: default
        - name: oauth-chain
          namespace: traefik
      services:
        - name: monitoring-kube-prometheus-prometheus
          port: 9090
  tls:
    secretName: prod-raulstechtips-tls
